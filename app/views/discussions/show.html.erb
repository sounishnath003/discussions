<p id="notice"><%= notice %></p>

<div class="container">
<div class="article is-centered ">
  <p>
    <strong>Title:</strong>
    <%= @discussion.title %>
  </p>

  <p>
    <strong>Content:</strong>
    <%= @discussion.content %>
  </p>

  <%= image_tag @discussion.ref_photo %>

</div>

</div>

<br>
<br>
<%= link_to "👏", discussion_likes_path(@discussion), method: :post %>
<p class="text-2xl text-black"><%= @discussion.likes.count %><%= @discussion.likes.count == 1 ? " Like" : " Likes" %> </p>
<br>
<br>
<%= link_to 'Edit', edit_discussion_path(@discussion) %> |
<%= link_to 'Back', discussions_path %>
